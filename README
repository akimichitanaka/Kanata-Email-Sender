This is Perl module Kanata::Email::Sender.

INSTALLATION

Kanata::Email::Sender installation is straightforward. If your CPAN shell is set up,
you should just be able to do

    % cpan Kanata::Email::Sender

Download it, unpack it, then build it as per the usual:

    % perl Makefile.PL
    % make && make test

Then install it:

    % make install

DOCUMENTATION

Kanata::Email::Sender documentation is available as in POD. So you can do:

    % perldoc Kanata::Email::Sender

to read the documentation online with your favorite pager.

akimichi
